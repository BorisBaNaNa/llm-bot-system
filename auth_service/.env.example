APP_NAME=auth-service
ENV=local

# ВАЖНО: JWT_SECRET должен совпадать с JWT_SECRET в bot_service,
# иначе бот не сможет проверить токены, выданные этим сервисом.
JWT_SECRET=change_me_super_secret
JWT_ALG=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

SQLITE_PATH=./auth.db
