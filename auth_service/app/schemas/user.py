"""Публичное представление пользователя.

UserPublic: id, email, role, created_at (с from_attributes). В схеме ответа
никогда не должно быть password_hash.
"""

# TODO: class UserPublic(BaseModel) ...
